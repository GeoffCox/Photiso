import type { UserSettings } from './types';

export const defaultUserSettings: UserSettings = {
	enableDefaultDirectoryName: true,
	defaultDirectoryName: 'yyyy/MM',
	enableDefaultFileName: true,
	defaultFileName: "'IMG_'yyyy-MM-dd_HH-mm-ss-SSS"
};
